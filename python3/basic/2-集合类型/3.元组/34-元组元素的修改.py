#
# tuple所谓的“不变”是说：tuple的每个元素，指向永远不变
#


# 1.tuple内的直接数据如果修改则立即报错
t1 = (10, 20, 30)
# t1[0] = 1


# 2.但是如果tuple里面有列表，修改列表里面的数据则是支持的!!!
t2 = (10, 20, 30, [40, 50])
print(t2)

t2[-1][0] = 999  # tuple最后一个元素指向的list并没有变，但list中的元素被修改了
print(t2)

t2[-1] = [1, 2, 3]  # 修改tuple最后一个元素指向的list，报错
